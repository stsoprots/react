Lessons - Lesson9 - TanStack query part 1

// React Query (TanStack Query) — це бібліотека-помічник для React, яка сама керує даними з сервера:
// вона завантажує їх, автоматично кешує (зберігає в пам'яті), оновлює у фоні
// і віддає готові змінні (data, isLoading, isError), щоб не писати купу useState та useEffect вручну.

------------------------------------------

// ==== Підсумок проєкту ======
// Весь цей проєкт — це простий клієнтський додаток, який:

// 1. Робить запити до сервера — через власну обгортку над Axios (useApi) звертається до бекенду за списком користувачів.

// 2. Керує даними через React Query — автоматично кешує отриманих юзерів, відстежує стан запиту (isFetching) та фоново оновлює дані при поверненні на вкладку.

// 3. Відображає результат — показує напис «Loading ...», поки дані вантажаться, а потім рендерить готовий список імен та ID на екрані.