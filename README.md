Lessons - Lesson9 - TanStack query part 3: Pagination

// React Query (TanStack Query) — це бібліотека-помічник для React, яка сама керує даними з сервера:
// вона завантажує їх, автоматично кешує (зберігає в пам'яті), оновлює у фоні
// і віддає готові змінні (data, isLoading, isError), щоб не писати купу useState та useEffect вручну.

------------------------------------------
------------------------------------------

// ==== Підсумок проєкту /tanstack-query-3 ======
// У цій частині додаток реалізує розбиття даних на сторінки (пагінацію):

// 1. Запитує порційні дані — хук useGetPaginatedPosts передає query-параметри (_limit та _start/offset) на бекенд для отримання потрібної пачки постів.

// 2. Кешує кожну сторінку окремо — формує динамічний queryKey ['posts', limit, offset], щоб переглянуті сторінки відкривалися миттєво з пам'яті.

// 3. Забезпечує плавний перехід — властивість keepPreviousData залишає попередні дані видимими, поки підвантажується наступна сторінка (без стрибків і блимання екрана).

// 4. Керує навігацією — компонент App.tsx змінює offset через кнопки («Перша», «Попередня», «Наступна», «Остання») і блокує їх на крайніх межах.

------------------------------------------

